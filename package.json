{
  "scripts": {
    "client": "npm start --prefix client",
    "server": "ts-node-dev --ignore-watch node_modules server/src/server.ts",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  }
}
