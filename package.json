{
  "scripts": {
    "client": "npm start --prefix client",
    "server": "ts-node-dev --transpileOnly --ignore-watch node_modules server/server.ts",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },

  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
